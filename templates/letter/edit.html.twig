{% extends 'base.html.twig' %}

{% block title %}Edit Letter{% endblock %}

{% block body %}
    <h1>Edit Letter</h1>

    {{ include('letter/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('letter_index') }}">back to list</a>

    {{ include('letter/_delete_form.html.twig') }}
{% endblock %}
