{% extends 'base.html.twig' %}

{% block title %}Edit Beoordeling{% endblock %}

{% block body %}
    <h1>Edit Beoordeling</h1>

    {{ include('beoordeling/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('beoordeling_index') }}">back to list</a>

    {{ include('beoordeling/_delete_form.html.twig') }}
{% endblock %}
